import { createElement } from "./index.js";
import { spellToIconsMap } from "../utils/spell-to-icons-map.js";

const createCooldownsBreakdown = (simulationData, containerCount) => {
    console.log(simulationData.results.cooldowns_breakdown)
};

export { createCooldownsBreakdown };
